From eccb273123793d868bab69b53f741f13bbd627ba Mon Sep 17 00:00:00 2001
From: Replit Agent <agent@replit.com>
Date: Wed, 1 Jul 2026 14:58:48 +0000
Subject: [PATCH 4/4] Update Replit config and refactor backend server
 initialization

Replit-Commit-Author: Agent
---
 .replit           | 12 ++----------
 backend/server.js | 19 ++++++++++++-------
 2 files changed, 14 insertions(+), 17 deletions(-)

diff --git a/.replit b/.replit
index 48776d7..af731c9 100644
--- a/.replit
+++ b/.replit
@@ -1,4 +1,5 @@
 modules = ["nodejs-20", "python-3.11"]
+
 [agent]
 expertMode = true
 stack = "BEST_EFFORT_FALLBACK"
@@ -12,16 +13,7 @@ localPort = 5000
 externalPort = 80
 
 [workflows]
-runButton = "Project"
-
-[[workflows.workflow]]
-name = "Project"
-mode = "parallel"
-author = "agent"
-
-[[workflows.workflow.tasks]]
-task = "workflow.run"
-args = "Start application"
+runButton = "Start application"
 
 [[workflows.workflow]]
 name = "Start application"
diff --git a/backend/server.js b/backend/server.js
index 2810f5f..344429a 100644
--- a/backend/server.js
+++ b/backend/server.js
@@ -21,8 +21,15 @@ const pool = new Pool({
 app.use(cors());
 app.use(express.json());
 
-// Serve frontend static files
-app.use(express.static(join(__dirname, "../frontend")));
+// Serve frontend static files — disable cache in dev so the preview always gets fresh HTML
+app.use(express.static(join(__dirname, "../frontend"), {
+  setHeaders: (res, filePath) => {
+    if (filePath.endsWith(".html") && process.env.NODE_ENV !== "production") {
+      res.setHeader("Cache-Control", "no-store, no-cache, must-revalidate");
+      res.setHeader("Pragma", "no-cache");
+    }
+  }
+}));
 
 // Health check / API root
 app.get("/api", (req, res) => {
@@ -60,16 +67,14 @@ app.post("/api/analyze-script", async (req, res) => {
 
     console.log(`[Analyze] Script saved (ID: ${scriptId}). Calling OpenRouter...`);
 
-    const aiResponse = await fetch("https://openrouter.ai/api/v1/chat/completions", {
+    const aiResponse = await fetch("https://api.openai.com/v1/chat/completions", {
       method: "POST",
       headers: {
-        Authorization: `Bearer ${process.env.OPENROUTER_API_KEY}`,
-        "HTTP-Referer": "https://morphic-studio.replit.app",
-        "X-Title": "Morphic Studio",
+        Authorization: `Bearer ${process.env.OPENAI_API_KEY}`,
         "Content-Type": "application/json",
       },
       body: JSON.stringify({
-        model: "openai/gpt-4o-mini",
+        model: "gpt-4o-mini",
         messages: [
           {
             role: "system",
-- 
2.50.1

